spring.application.name=courses

mysql.driver-class-name = com.mysql.cj.jdbc.Driver

## Connectivity to MySQL, to create session object to MySQL server
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.session.timeout.seconds=600
spring.h2.console.enabled=true

## Hibernate Logging
logging.level.org.hibernate.SQL=DEBUG

####### Spring Boot its own properties to create tables and DB starts from here #######
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url = jdbc:mysql://localhost:3306/courses_db?createDatabaseIfNotExist=true
#spring.datasource.username = root
#spring.datasource.password = T22051@gmandi
spring.datasource.driverClass-name=com.mysql.cj.jdbc.Driver
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.show_sql = true
spring.jpa.properties.hibernate.format_sql = true

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update